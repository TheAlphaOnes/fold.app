diff --git a/android/src/main/java/expo/modules/pasteinput/ExpoPasteInputView.kt b/android/src/main/java/expo/modules/pasteinput/ExpoPasteInputView.kt
index 99b0fa68814e57404d4c7f97c7fb47ced5ca59f1..43823d83dded3f6c4c668e9308af3be5491630b2 100644
--- a/android/src/main/java/expo/modules/pasteinput/ExpoPasteInputView.kt
+++ b/android/src/main/java/expo/modules/pasteinput/ExpoPasteInputView.kt
@@ -144,16 +144,14 @@ class ExpoPasteInputView(context: Context, appContext: AppContext) : ExpoView(co
   }
   
   private fun setupPasteHandling(editText: EditText) {
-    // Set up OnReceiveContentListener for Android 12+ (API 31+)
+    // Set up OnReceiveContentListener for Android (using ViewCompat for backward compatibility)
     // This is the primary mechanism for handling image pastes
-    if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.S) {
-      contentListener = createContentListener()
-      ViewCompat.setOnReceiveContentListener(
-        editText,
-        arrayOf("image/*", "text/plain"),
-        contentListener!!
-      )
-    }
+    contentListener = createContentListener()
+    ViewCompat.setOnReceiveContentListener(
+      editText,
+      arrayOf("image/*", "text/plain"),
+      contentListener!!
+    )
     
     // Intercept paste from context menu to prevent toast
     enhanceOnTextContextMenuItem(editText)
@@ -161,7 +159,7 @@ class ExpoPasteInputView(context: Context, appContext: AppContext) : ExpoView(co
   
   private fun cleanupPasteHandling(editText: EditText) {
     // Remove OnReceiveContentListener
-    if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.S && contentListener != null) {
+    if (contentListener != null) {
       ViewCompat.setOnReceiveContentListener(editText, null, null)
     }
     
